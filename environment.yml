name: iqs_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pandas
  - numpy
  - scipy
  - pytest
  - mypy
  - pip
  - pip:
      - ib_insync
      - groq
      - pybind11
      - yfinance
      - python-dotenv